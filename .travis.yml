language: python

install:
  install: "pip install -r ./requirements/requirements.txt"

node_js:
  - "stable"

before_script:
  - npm install -g grunt-cli
  - npm install
  - pip install six tox

script:
  - npm test
  - tox --sitepackages

after_success:
    - coveralls
